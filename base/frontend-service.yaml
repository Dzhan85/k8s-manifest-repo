---
apiVersion: v1
kind: Service
metadata:
  name: maverick-ui
  namespace: dev
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: "/"
spec:
  selector:
    app: maverick-ui-dev
  type: LoadBalancer #NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
