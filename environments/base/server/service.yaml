apiVersion: v1
kind: Service
metadata:
  name: kustomization-microservices-ui
  labels:
    app: kustomization-microservices-ui
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 443
      targetPort: 3000
  selector:
    app: kustomization-microservices-ui
