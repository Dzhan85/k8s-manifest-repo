apiVersion: v1
kind: Service
metadata:
  name: kustomization-microservices-ui
  labels:
    app: kustomization-microservices-ui
spec:
  type: LoadBalancer
  ports:
    - port: 3000
  selector:
    app: kustomization-microservices-ui
